import Balance from "../Balance/Balance";

const RightSection = () => {
  return (
    <section className="sticky top-0 border">
      <Balance />
    </section>
  );
};

export default RightSection;
